import { TestcaseFilterPipe } from './testcase-filter-pipe';

describe('TestcaseFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TestcaseFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
